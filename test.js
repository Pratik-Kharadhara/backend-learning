// const express = require('express'); //this line intiliaze the express

// const app = express(); //this helps to run the server

// app.get("/",(req, res)=>{ // '/' means the initial path/home path 
//     res.send('Welcom to the server');
// })

// app.get("/favourites",(req,res)=>{ // when a user request for the  '/favourites' it send the response below  
//     res.send('welcome to the favourite page');
// })

// app.listen(3000) //the 3000 is the port number for our localhost server
